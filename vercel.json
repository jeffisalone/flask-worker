{
  "version": 2,
  "builds": [
    {
      "src": "app.py",  // 你的Flask主文件（比如app.py）
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"  // 对应Flask主文件
    }
  ]
}
